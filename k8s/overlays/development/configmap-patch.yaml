apiVersion: v1
kind: ConfigMap
metadata:
  name: wanllmdb-config
  namespace: wanllmdb-dev
data:
  # Override for development environment
  APP_ENV: "development"
  DEBUG: "true"
  LOG_LEVEL: "DEBUG"
  CORS_ORIGINS: "http://localhost:3000,http://localhost:5173,http://dev.wanllmdb.local"
